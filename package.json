{
  "name": "lttp-menu-simulator",
  "private": true,
  "scripts": {
    "build": "babel src -d js",
    "watch": "watch \"npm run build\""
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "watch": "^1.0.2"
  },
  "babel": {
    "presets": [
      ["env", { "modules": false }],
      "react"
    ]
  }
}
